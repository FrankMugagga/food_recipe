<!DOCTYPE html>
<html>
  <head>
    <title>FoodRecipe</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload", media: 'all' %>
    <%= javascript_importmap_tags %>
  </head>

  <body> 
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <% if user_signed_in? %>
      <div class="logout">
        <div> <%= image_tag 'recipe.jpeg', alt: 'recipe_app_image', class: 'image' %> Recipes App </div>
        <%= button_to "Logout", destroy_user_session_path, method: :delete, class: 'submit' %>        
      </div>      
    <% end %>

    <%= yield %>
  </body>
</html>
