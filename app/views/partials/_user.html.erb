<%= user.name %> 's 
<%= link_to 'Public recipes', user %>
<%= user.email %>
<% if can? :manage, user %>
  <%= link_to 'Delete', user, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } %>
<% end %>

